<%- include('../partials/header') %>

  <div class="starter-template">
    <h1>Notícias Privadas</h1>
    <table>
      <% news.forEach( newsfree => { %>
      <tr>
        <td><%= newsfree.title %></td>
      </tr>
      <% }) %>
    </table>
  </div>

<%- include('../partials/footer') %>